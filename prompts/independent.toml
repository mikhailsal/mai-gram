# Per-prompt config for "independent".
# The independent companion feels more natural without visible reasoning
# or tool call details. All tools remain available under the hood.

show_reasoning = false
show_tool_calls = false
